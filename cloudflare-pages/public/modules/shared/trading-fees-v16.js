export const TRADING_FEE_RATES=Object.freeze({bithumb_krw:0.0004,bithumb_btc:0,upbit_krw:0.0005});
export function holdingQuoteCurrency(market){const value=String(market||'').trim().toUpperCase(),pair=value.includes('-')?value.split('-',2)[1]:value;if(pair.includes('/'))return pair.split('/',2)[1]||'KRW';return value.startsWith('BTC-')?'BTC':value.startsWith('USDT-')?'USDT':'KRW'}
export function normalizeFeeExchange(value){const exchange=String(value||'').toLowerCase();return exchange==='upbit'?'upbit':exchange==='bithumb'?'bithumb':''}
export function tradingFeeProfile(exchange,market='KRW-'){const ex=normalizeFeeExchange(exchange),quote=holdingQuoteCurrency(market);if(ex==='bithumb'&&quote==='BTC')return{exchange:ex,quote,rate:0,label:'빗썸 BTC 마켓 0%'};if(ex==='bithumb'&&quote==='KRW')return{exchange:ex,quote,rate:0.0004,label:'빗썸 쿠폰 0.04%'};if(ex==='upbit'&&quote==='KRW')return{exchange:ex,quote,rate:0.0005,label:'업비트 KRW 0.05%'};return null}
export function tradingFeeRate(exchange,market='KRW-'){const profile=tradingFeeProfile(exchange,market);return profile?profile.rate:NaN}
export function tradingFeeLabel(exchange,market='KRW-'){return tradingFeeProfile(exchange,market)?.label||'수수료 정책 확인 필요'}
export function tradingFee(amount,exchange,market='KRW-'){const value=Math.max(0,Number(amount)||0),rate=tradingFeeRate(exchange,market);return Number.isFinite(rate)?value*rate:0}
export function tradingCost({buyGross=0,sellGross=0,exchange='bithumb',market='KRW-'}={}){const buy=Math.max(0,Number(buyGross)||0),sell=Math.max(0,Number(sellGross)||0),rate=tradingFeeRate(exchange,market),buyFee=Number.isFinite(rate)?buy*rate:0,sellFee=Number.isFinite(rate)?sell*rate:0;return{exchange:normalizeFeeExchange(exchange),quote:holdingQuoteCurrency(market),rate,buy_fee:buyFee,sell_fee:sellFee,total_fee:buyFee+sellFee,buy_total:buy+buyFee,sell_net:sell-sellFee}}
