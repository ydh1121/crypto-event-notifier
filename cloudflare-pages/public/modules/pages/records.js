import{recordsData}from'../shared/selectors.js';
import{pageHead,loading,empty,exchangeToggle,kpi}from'../shared/components.js';
import{n,money,pct,tone,esc,dt,age,price}from'../shared/format.js';

export function createRecordsPage({store}){
  let root=null,unsub=null,seenKeys=new Set();
  function recordKey(row){return`${row.kind}|${row.strategy||''}|${n(row.ts)}|${row.market||''}|${n(row.price)}|${n(row.realized_pnl)}|${n(row.outcome_return_pct)}`}
  function items(){const d=recordsData(store.get(),store.get().ui.recordsExchange),fills=(Array.isArray(d.fills)?d.fills:[]).map(x=>({...x,kind:x.side==='sell'?'sell':'buy'})),feedback=(Array.isArray(d.feedback)?d.feedback:[]).map(x=>({...x,kind:'learning'}));return{data:d,all:[...fills,...feedback].sort((a,b)=>n(b.ts)-n(a.ts))}}
  function strategyOptions(all){return[...new Set(all.map(x=>String(x.strategy||'adaptive').toLowerCase()).filter(Boolean))].sort((a,b)=>a.localeCompare(b))}
  function filteredItems(all){const ui=store.get().ui,f=ui.recordsFilter,strategy=String(ui.recordsStrategy||'all').toLowerCase(),q=String(ui.recordsSearch||'').trim().toLowerCase(),period=ui.recordsPeriod||'all',seconds={h1:3600,h6:21600,d1:86400,d7:604800}[period]||0,cutoff=seconds?Date.now()/1000-seconds:0;return all.filter(x=>{if(f!=='all'&&x.kind!==f)return false;if(strategy!=='all'&&String(x.strategy||'adaptive').toLowerCase()!==strategy)return false;if(cutoff&&n(x.ts)<cutoff)return false;if(q&&!`${x.symbol||''} ${x.market||''}`.toLowerCase().includes(q))return false;return true})}
  function model(){const {data,all}=items(),filtered=filteredItems(all),buy=all.filter(x=>x.kind==='buy').length,sell=all.filter(x=>x.kind==='sell').length,learning=all.filter(x=>x.kind==='learning').length,realized=filtered.filter(x=>x.kind==='sell').reduce((s,x)=>s+n(x.realized_pnl),0);return{data,all,filtered,buy,sell,learning,realized,strategies:strategyOptions(all)}}
  function kpis(m){return`${kpi('누적 체결',`${n(m.data.fill_count).toLocaleString('ko-KR')}건`,{sub:`최근 표시 ${m.buy}매수 · ${m.sell}매도`})}${kpi('누적 학습',`${n(m.data.feedback_count).toLocaleString('ko-KR')}건`)}${kpi('필터 구간 매도손익',`${m.realized>=0?'+':''}${money(m.realized)}`,{toneClass:tone(m.realized),sub:`현재 ${m.filtered.length}건 기준`})}${kpi('마지막 기록',age(m.data.updated_at),{sub:dt(m.data.updated_at)})}`}
  function insight(m){const lastBuy=m.all.find(x=>x.kind==='buy'),lastSell=m.all.find(x=>x.kind==='sell'),lastLearning=m.all.find(x=>x.kind==='learning'),wins=m.all.filter(x=>x.kind==='sell'&&n(x.realized_pnl)>0).length,losses=m.all.filter(x=>x.kind==='sell'&&n(x.realized_pnl)<0).length;return`<header><div><h3>기록 요약</h3><p>넓은 테이블 대신 최근 핵심 상태만 따로 봅니다.</p></div><span>${store.get().ui.recordsExchange==='upbit'?'업비트':'빗썸'}</span></header><div class="records-insight-kpis"><span><small>최근 매도 승/패</small><b>${wins} / ${losses}</b></span><span><small>현재 필터</small><b>${m.filtered.length}건</b></span></div>${insightRow('최근 매수',lastBuy,lastBuy?`${money(lastBuy.krw)} · ${price(lastBuy.price)}`:'기록 없음')}${insightRow('최근 매도',lastSell,lastSell?`${n(lastSell.realized_pnl)>=0?'+':''}${money(lastSell.realized_pnl)}`:'기록 없음',lastSell?tone(lastSell.realized_pnl):'')}${insightRow('최근 학습',lastLearning,lastLearning?`결과 ${pct(lastLearning.outcome_return_pct)}`:'기록 없음',lastLearning?tone(lastLearning.outcome_return_pct):'')}<p class="records-live-note"><i></i> 새 기록은 화면 새로고침 없이 맨 위에 추가됩니다.</p>`}
  function insightRow(label,row,value,toneClass=''){return`<div class="records-insight-row"><span><small>${label}</small><b>${row?esc(String(row.symbol||row.market||'').replace(/^KRW-/,'')):'-'}</b></span><span class="${toneClass}">${value}</span>${row?`<time>${dt(row.ts)}</time>`:''}</div>`}
  function strategySelect(m,selected){return`<select data-records-strategy aria-label="전략"><option value="all" ${selected==='all'?'selected':''}>전체 전략</option>${m.strategies.map(value=>`<option value="${esc(value)}" ${selected===value?'selected':''}>${esc(value)}</option>`).join('')}</select>`}
  function render(){if(!root)return;const state=store.get();if(!state.snapshot){root.innerHTML=pageHead('기록','체결·판단·학습 이력을 거래소별로 추적합니다.')+loading('기록 데이터를 불러오는 중입니다.');return}const ex=state.ui.recordsExchange,m=model(),f=state.ui.recordsFilter,strategy=String(state.ui.recordsStrategy||'all').toLowerCase();root.innerHTML=`${pageHead('기록','체결·판단·학습 이력을 거래소별로 추적합니다.',exchangeToggle(ex,'data-records-exchange'))}<section class="records-kpis" data-records-kpis>${kpis(m)}</section><section class="records-toolbar"><input data-records-search type="search" value="${esc(state.ui.recordsSearch||'')}" placeholder="코인 검색">${strategySelect(m,strategy)}<select data-records-period aria-label="기간"><option value="all" ${state.ui.recordsPeriod==='all'?'selected':''}>전체 기간</option><option value="h1" ${state.ui.recordsPeriod==='h1'?'selected':''}>최근 1시간</option><option value="h6" ${state.ui.recordsPeriod==='h6'?'selected':''}>최근 6시간</option><option value="d1" ${state.ui.recordsPeriod==='d1'?'selected':''}>최근 24시간</option><option value="d7" ${state.ui.recordsPeriod==='d7'?'selected':''}>최근 7일</option></select><button data-records-reset>필터 초기화</button></section><div class="chip-row records-filter"><button data-records-filter="all" class="${f==='all'?'active':''}">전체 ${m.all.length}</button><button data-records-filter="buy" class="${f==='buy'?'active':''}">매수 ${m.buy}</button><button data-records-filter="sell" class="${f==='sell'?'active':''}">매도 ${m.sell}</button><button data-records-filter="learning" class="${f==='learning'?'active':''}">학습 ${m.learning}</button></div><section class="records-content-grid"><div class="records-feed" data-records-feed>${m.filtered.length?m.filtered.slice(0,120).map(row=>rowHtml(row,false)).join(''):empty('표시할 기록이 없습니다.','기간·코인·종류·전략 필터를 확인하세요.')}</div><aside class="records-insight" data-records-insight>${insight(m)}</aside></section>`;seenKeys=new Set(m.filtered.slice(0,120).map(recordKey))}
  function rowHtml(row,animate=false){const symbol=String(row.symbol||row.market||'').replace(/^KRW-/,''),strategy=String(row.strategy||'adaptive'),key=recordKey(row),cls=animate?' live-enter':'';if(row.kind==='learning'){const c=row.profile_change||{};return`<article class="record-item${cls}" data-record-key="${esc(key)}"><span class="record-type learning">학습</span><div><b>${esc(symbol)}</b><small>${esc(row.market||'')} · ${esc(strategy)}</small></div><div><b>완료 거래를 반영해 기준값 조정</b><p>시장 ${n(c.regime_before).toFixed(1)} → ${n(c.regime_after).toFixed(1)} · 진입 ${n(c.entry_before).toFixed(1)} → ${n(c.entry_after).toFixed(1)} · 비중 ${n(c.weight_before).toFixed(1)}% → ${n(c.weight_after).toFixed(1)}%</p></div><div><b class="${tone(row.outcome_return_pct)}">${pct(row.outcome_return_pct)}</b><small>${dt(row.ts)}</small></div></article>`}const sell=row.kind==='sell',amount=sell?n(row.realized_pnl):n(row.krw);return`<article class="record-item${cls}" data-record-key="${esc(key)}"><span class="record-type ${row.kind}">${sell?'매도':'매수'}</span><div><b>${esc(symbol)}</b><small>${esc(row.market||'')} · ${esc(strategy)}</small></div><div><b>${sell?'가상매도 체결':'가상매수 체결'} · ${price(row.price)}</b><p>${esc(row.reason||'')}</p></div><div><b class="${sell?tone(amount):''}">${sell?`${amount>=0?'+':''}${money(amount)}`:money(amount)}</b><small>${dt(row.ts)}</small></div></article>`}
  function refreshLive({animate=true}={}){if(!root||!store.get().snapshot)return;const m=model(),kpiBox=root.querySelector('[data-records-kpis]'),insightBox=root.querySelector('[data-records-insight]'),feed=root.querySelector('[data-records-feed]');if(kpiBox)kpiBox.innerHTML=kpis(m);if(insightBox)insightBox.innerHTML=insight(m);if(!feed)return;const rows=m.filtered.slice(0,120);if(!rows.length){feed.innerHTML=empty('표시할 기록이 없습니다.','기간·코인·종류·전략 필터를 확인하세요.');seenKeys.clear();return}const current=new Map([...feed.querySelectorAll('[data-record-key]')].map(node=>[node.dataset.recordKey,node])),desired=new Set(rows.map(recordKey));for(const [key,node] of current)if(!desired.has(key))node.remove();for(const row of [...rows].reverse()){const key=recordKey(row);let node=current.get(key);if(!node){const holder=document.createElement('div');holder.innerHTML=rowHtml(row,animate&&seenKeys.size>0);node=holder.firstElementChild}if(node)feed.prepend(node)}seenKeys=new Set(rows.map(recordKey))}
  const click=e=>{const ex=e.target.closest('[data-records-exchange]');if(ex){store.setUi({recordsExchange:ex.dataset.recordsExchange,recordsStrategy:'all'},{scope:'records'});seenKeys.clear();render();return}const f=e.target.closest('[data-records-filter]');if(f){store.setUi({recordsFilter:f.dataset.recordsFilter},{scope:'records'});root.querySelectorAll('[data-records-filter]').forEach(x=>x.classList.toggle('active',x===f));refreshLive({animate:false});return}if(e.target.closest('[data-records-reset]')){store.setUi({recordsFilter:'all',recordsPeriod:'all',recordsSearch:'',recordsStrategy:'all'},{scope:'records'});render()}};
  const input=e=>{if(e.target.matches('[data-records-search]')){store.setUi({recordsSearch:e.target.value},{scope:'records'});refreshLive({animate:false})}};
  const change=e=>{if(e.target.matches('[data-records-period]')){store.setUi({recordsPeriod:e.target.value},{scope:'records'});refreshLive({animate:false});return}if(e.target.matches('[data-records-strategy]')){store.setUi({recordsStrategy:e.target.value},{scope:'records'});refreshLive({animate:false})}};
  return{mount(r){root=r;root.addEventListener('click',click);root.addEventListener('input',input);root.addEventListener('change',change);unsub=store.subscribe((_,m)=>{if(m.type==='snapshot')refreshLive({animate:true})})},render,destroy(){unsub?.();root?.removeEventListener('click',click);root?.removeEventListener('input',input);root?.removeEventListener('change',change);root=null}};
}