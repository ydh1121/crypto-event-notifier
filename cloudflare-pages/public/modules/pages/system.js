import{fullPublic}from'../shared/selectors.js';
import{pageHead,loading}from'../shared/components.js';
import{esc,dt,n}from'../shared/format.js';
import{request}from'../core/http.js';

const statusLabel=value=>({healthy:'정상',running:'실행 중',starting:'시작 중',stopped:'중지',offline:'오프라인',degraded:'확인 필요'}[String(value||'').toLowerCase()]||String(value||'확인'));
const yesNo=value=>value?'예':'아니오';
const byteSize=value=>{const x=Math.max(0,n(value));if(x>=1048576)return`${(x/1048576).toFixed(2)} MB`;if(x>=1024)return`${(x/1024).toFixed(1)} KB`;return`${Math.round(x).toLocaleString('ko-KR')} B`};
const duration=value=>{const x=Math.max(0,n(value));return x?`${x.toFixed(x<10?2:1)}초`:'-'};
const CI_REPO='ydh1121/crypto-event-notifier',CI_BRANCH='b3-auto-trader-phase1',CI_TTL_MS=300000;
let ciCache={fetchedAt:0,status:'idle',runs:[],error:''},ciPromise=null;
function safeResultPairs(result){if(!result||typeof result!=='object')return[];const labels={status:'상태',processed:'처리',stored:'저장',published:'전송',retries:'재시도',bytes:'용량',total_bytes:'총 용량',requests:'요청',exported_rows:'내보낸 행',experiment_count:'전략 실험',strategy_lab_experiments:'전략 실험',market_total:'시장',source_rows:'소스 행',trades:'체결',health_ok:'Health',changed_files:'변경 파일',elapsed_seconds:'소요 시간'};const out=[];for(const[key,label]of Object.entries(labels)){if(!(key in result))continue;let value=result[key];if(key.includes('bytes'))value=byteSize(value);else if(key==='elapsed_seconds')value=duration(value);else if(typeof value==='boolean')value=yesNo(value);else if(typeof value==='number')value=value.toLocaleString('ko-KR');else value=String(value);out.push([label,value]);if(out.length>=7)break}return out}
function componentRows(node){return Array.isArray(node?.components)?node.components:Array.isArray(node?.component_status)?node.component_status:[]}
function operationCards(node){const ops=node?.operations||{},git=ops.git||{},cf=ops.cloudflare||{},snap=cf.snapshot||{},detail=cf.market_details||{},pages=cf.pages||{},wh=ops.warehouse||{},backup=ops.backup||{},tg=ops.telegram||{},remote=ops.remote_access||{},remoteCf=remote.cloudflare||{},tailscale=remote.tailscale||{};return`<section class="operations-grid"><article class="operation-card"><header><div><span>Git</span><h3>코드 동기화</h3></div><em class="${git.aligned?'good':'warn'}">${git.aligned?'동기화됨':'확인 필요'}</em></header><div class="operation-primary"><b>${esc(git.local_head||'-')}</b><small>${esc(git.branch||git.configured_branch||'-')}</small></div><div class="operation-facts"><span><small>origin</small><b>${esc(git.origin_head||'-')}</b></span><span><small>자동 동기화</small><b>${yesNo(git.auto_sync_enabled)}</b></span></div><p>Viewer에서 Git을 pull/reset/push하지 않습니다.</p></article><article class="operation-card"><header><div><span>Cloudflare</span><h3>Pages · 데이터 전송</h3></div><em class="${pages.health_ok?'good':'warn'}">${pages.health_ok?'Health 정상':'확인 필요'}</em></header><div class="operation-primary"><b>${esc(pages.head||'-')}</b><small>${esc(pages.status||'unknown')}</small></div><div class="operation-facts"><span><small>Snapshot</small><b>${byteSize(snap.bytes)} · 재시도 ${n(snap.retries)}</b></span><span><small>코인 상세</small><b>${n(detail.published)}건 · ${n(detail.requests)}요청</b></span></div><p>${pages.viewer_url?esc(pages.viewer_url):'Viewer URL 대기'}</p></article><article class="operation-card"><header><div><span>Warehouse</span><h3>Parquet 분석 창고</h3></div><em class="${wh.status==='ok'||wh.status==='ready'?'good':'neutral'}">${esc(wh.status||'대기')}</em></header><div class="operation-primary"><b>${n(wh.rows_exported_total).toLocaleString('ko-KR')}행</b><small>${esc(wh.format||'Parquet/ZSTD')}</small></div><div class="operation-facts"><span><small>추적 테이블</small><b>${n(wh.tracked_tables)}개</b></span><span><small>최근 실행</small><b>${n(wh.last_run_rows).toLocaleString('ko-KR')}행</b></span></div><p>실시간 원본은 ${esc(wh.authoritative_store||'SQLite')}이며 Warehouse는 분석용 2차 저장소입니다.</p></article><article class="operation-card"><header><div><span>Backup</span><h3>SQLite · Drive 백업</h3></div><em class="${backup.drive_uploaded?'good':backup.status==='error'?'warn':'neutral'}">${backup.drive_uploaded?'Drive 완료':backup.drive_configured?'Drive 확인':'로컬 백업'}</em></header><div class="operation-primary"><b>${n(backup.local_backup_count).toLocaleString('ko-KR')}개</b><small>${backup.last_backup_at?`최근 ${dt(backup.last_backup_at)}`:'백업 대기'}</small></div><div class="operation-facts"><span><small>최근 파일</small><b>${byteSize(backup.latest_local_bytes)}</b></span><span><small>Drive</small><b>${backup.drive_uploaded?'업로드 완료':backup.drive_configured?esc(backup.drive_status||'설정됨'):'미설정'}</b></span></div><p>로컬 경로와 rclone 원격 경로는 Viewer에 노출하지 않습니다.</p></article><article class="operation-card"><header><div><span>Telegram</span><h3>BUY_CANDIDATE 알림</h3></div><em class="${tg.ready?'good':tg.enabled?'warn':'neutral'}">${tg.ready?'준비됨':tg.enabled?'설정 확인':'비활성'}</em></header><div class="operation-primary"><b>${tg.buy_candidate_only?'매수 후보만 알림':'알림 모드 확인'}</b><small>${tg.last_buy_candidate_sent_at?`마지막 ${dt(tg.last_buy_candidate_sent_at)}`:'아직 자동 알림 없음'}</small></div><div class="operation-facts"><span><small>전송 건수</small><b>${n(tg.buy_candidate_sent_count).toLocaleString('ko-KR')}건</b></span><span><small>Bot / Chat</small><b>${tg.token_configured&&tg.chat_configured?'설정됨':'미완료'}</b></span></div><p>Bot token과 Chat ID 실제 값은 표시하거나 전송하지 않습니다.</p></article><article class="operation-card"><header><div><span>Remote access</span><h3>휴대폰 · 원격 접속</h3></div><em class="${remoteCf.active?'good':remote.local_dashboard_online?'neutral':'warn'}">${remoteCf.active?'HTTPS 활성':remote.local_dashboard_online?'로컬만':'확인 필요'}</em></header><div class="operation-primary"><b>${remoteCf.active?'Cloudflare 연결':remote.local_dashboard_online?'로컬 대시보드':'상태 대기'}</b><small>${remoteCf.active?esc(remoteCf.mode||'tunnel'):'외부 연결 비활성'}</small></div><div class="operation-facts"><span><small>Tailscale</small><b>${tailscale.connected?'연결됨':tailscale.installed?'미연결':'미설치'}</b></span><span><small>원격 인증</small><b>${remote.remote_auth_required?'필수':'확인 필요'}</b></span></div><p>IP·DNS·접속 토큰은 공개하지 않으며 Viewer에서 PC를 제어할 수 없습니다.</p></article></section>`}
function componentDiagnostics(components){return components.length?`<section class="section-panel component-panel"><header><div><h3>실행 구성요소</h3><p>각 사이드카의 주기·실행 횟수·최근 성공과 안전한 결과 요약입니다.</p></div><span>${components.length}개</span></header><div class="component-diagnostics">${components.map(c=>{const pairs=safeResultPairs(c.last_result);return`<details class="component-diagnostic"><summary><div><i class="component-dot ${esc(c.status||'offline')}"></i><span><b>${esc(c.label||c.name||'component')}</b><small>${esc(c.description||'')}</small></span></div><strong>${statusLabel(c.status)}</strong></summary><div class="component-detail-grid"><span><small>구성 ID</small><b>${esc(c.name||'-')}</b></span><span><small>실행 주기</small><b>${n(c.interval_seconds).toLocaleString('ko-KR')}초</b></span><span><small>실행 횟수</small><b>${n(c.runs).toLocaleString('ko-KR')}회</b></span><span><small>마지막 성공</small><b>${c.last_success_at?dt(c.last_success_at):'-'}</b></span></div>${c.last_error?`<div class="component-error"><b>최근 오류</b><span>${esc(c.last_error)}</span></div>`:''}${pairs.length?`<div class="component-result">${pairs.map(([label,value])=>`<span><small>${esc(label)}</small><b>${esc(value)}</b></span>`).join('')}</div>`:'<p class="component-empty">아직 표시할 실행 결과가 없습니다.</p>'}</details>`}).join('')}</div></section>`:''}
function ciSummary(){const runs=ciCache.runs||[];if(ciCache.status==='loading'&&!runs.length)return`<section class="section-panel" data-ci-panel><header><div><h3>GitHub Actions CI</h3><p>${esc(CI_BRANCH)} 브랜치의 최근 workflow 상태입니다.</p></div><span>조회 중</span></header><p class="component-empty">GitHub의 공개 CI 상태를 불러오는 중입니다.</p></section>`;if(ciCache.status==='error'&&!runs.length)return`<section class="section-panel" data-ci-panel><header><div><h3>GitHub Actions CI</h3><p>${esc(CI_BRANCH)} 브랜치의 최근 workflow 상태입니다.</p></div><span>확인 필요</span></header><div class="component-error"><b>CI 상태 조회 실패</b><span>${esc(ciCache.error||'GitHub API 응답을 확인하세요.')}</span></div><p class="component-empty">이 오류는 매매·PAPER·데이터 수집 상태와는 별개입니다.</p></section>`;if(!runs.length)return`<section class="section-panel" data-ci-panel><header><div><h3>GitHub Actions CI</h3><p>${esc(CI_BRANCH)} 브랜치의 최근 workflow 상태입니다.</p></div><span>대기</span></header><p class="component-empty">표시할 최근 workflow가 없습니다.</p></section>`;const pending=runs.filter(r=>r.status!=='completed'),failed=runs.filter(r=>r.status==='completed'&&!['success','neutral','skipped'].includes(r.conclusion)),overall=pending.length?'실행 중':failed.length?`${failed.length}개 실패`:'통과',overallClass=pending.length?'neutral':failed.length?'warn':'good';return`<section class="section-panel" data-ci-panel><header><div><h3>GitHub Actions CI</h3><p>${esc(CI_BRANCH)} · 공개 GitHub API 읽기 전용 · 5분 캐시</p></div><span class="${overallClass}">${overall}</span></header><div class="component-diagnostics">${runs.slice(0,8).map(r=>{const done=r.status==='completed',ok=done&&['success','neutral','skipped'].includes(r.conclusion),label=!done?'실행 중':ok?'PASS':'FAIL',cls=!done?'running':ok?'healthy':'degraded';return`<div class="component-diagnostic"><summary><div><i class="component-dot ${cls}"></i><span><b>${esc(r.name||'workflow')}</b><small>#${n(r.run_number)} · ${r.updated_at?dt(Date.parse(r.updated_at)/1000):'-'}</small></span></div><strong>${label}</strong></summary></div>`}).join('')}</div></section>`}
async function loadCiStatus(){const now=Date.now();if(ciCache.fetchedAt&&now-ciCache.fetchedAt<CI_TTL_MS)return ciCache;if(ciPromise)return ciPromise;ciCache={...ciCache,status:'loading'};ciPromise=(async()=>{try{const url=`https://api.github.com/repos/${CI_REPO}/actions/runs?branch=${encodeURIComponent(CI_BRANCH)}&per_page=50`;const response=await fetch(url,{headers:{Accept:'application/vnd.github+json'}});if(!response.ok)throw new Error(`GitHub API ${response.status}`);const payload=await response.json(),source=Array.isArray(payload?.workflow_runs)?payload.workflow_runs:[],latest=new Map();for(const run of source){const name=String(run?.name||'workflow');if(!latest.has(name))latest.set(name,{name,run_number:n(run?.run_number),status:String(run?.status||''),conclusion:String(run?.conclusion||''),updated_at:run?.updated_at||''})}ciCache={fetchedAt:Date.now(),status:'ready',runs:[...latest.values()],error:''};return ciCache}catch(err){ciCache={...ciCache,fetchedAt:Date.now(),status:'error',error:String(err?.message||err||'GitHub API error').slice(0,180)};return ciCache}finally{ciPromise=null}})();return ciPromise}

export function createSystemPage({store}){
  let root=null,unsub=null;
  async function refreshCi(){await loadCiStatus();if(root){const current=root.querySelector('[data-ci-panel]');if(current){const holder=document.createElement('div');holder.innerHTML=ciSummary();current.replaceWith(holder.firstElementChild)}}}
  function render(){if(!root)return;const state=store.get(),user=state.user;if(!state.snapshot){root.innerHTML=pageHead('시스템','연구 노드, 데이터 수집, 배포와 계정 상태를 확인합니다.')+loading('시스템 상태를 불러오는 중입니다.');return}const pub=fullPublic(state),node=pub.research_node||{},components=componentRows(node);root.innerHTML=`${pageHead('시스템','투자 판단 화면과 분리된 운영·계정 영역입니다.')}${operationCards(node)}${ciSummary()}<section class="system-grid"><article class="section-panel"><header><h3>연구 노드</h3><span>${node.supervisor_running===false?'확인 필요':'정상'}</span></header><div class="system-list"><div><span>Supervisor</span><b>${node.supervisor_running===false?'중지':'실행 중'}</b></div><div><span>PID</span><b>${n(node.supervisor_pid)||'-'}</b></div><div><span>마지막 상태 갱신</span><b>${dt(node.updated_at||pub.source_updated_at||pub.published_at)}</b></div><div><span>구성요소</span><b>${components.length}개</b></div><div><span>Control revision</span><b>${n(node.control_revision)}</b></div><div><span>외부 레포 업데이트</span><b>${n(node.references?.updates)}건</b></div><div><span>외부 레포 오류</span><b>${n(node.references?.failed)}건</b></div></div></article><article class="section-panel"><header><h3>내 계정</h3><span>${user?.role==='owner'?'관리자':'조회 사용자'}</span></header><div class="system-list"><div><span>이름</span><b>${esc(user?.display_name||user?.email||'-')}</b></div><div><span>권한</span><b>${user?.role==='owner'?'관리자':'조회 사용자'}</b></div><div><span>내 자산정보</span><b>${user?.role==='owner'||user?.can_view_holdings?'조회 가능':'권한 없음'}</b></div><div><span>외부 Viewer</span><b>READ ONLY</b></div><div><span>PC 제어</span><b>불가</b></div><div><span>실제 주문</span><b>불가</b></div></div></article></section>${componentDiagnostics(components)}${user?.role==='owner'?`<section class="section-panel"><header><h3>사용자 초대</h3><span>관리자 전용</span></header><form id="systemInviteForm" class="invite-form"><label>이메일<input id="systemInviteEmail" type="email" required></label><label class="check"><input id="systemInviteHoldings" type="checkbox"> 내 자산정보 조회 허용</label><button type="submit">초대 링크 만들기</button></form><div id="systemInviteResult"></div></section>`:''}<section class="system-safety"><h3>안전 계약</h3><div><span>PAPER ONLY</span><span>외부 Viewer READ ONLY</span><span>Cloudflare → PC 제어 없음</span><span>실제 주문 없음</span><span>비밀값 미노출</span><span>IP·원격 경로 미노출</span><span>Git 원격 변경 없음</span></div></section>`;void refreshCi()}
  const submit=async e=>{if(e.target.id!=='systemInviteForm')return;e.preventDefault();try{const data=await request('/api/invites/create',{method:'POST',body:{email:root.querySelector('#systemInviteEmail').value.trim(),can_view_holdings:root.querySelector('#systemInviteHoldings').checked}});const link=`${location.origin}/#invite=${encodeURIComponent(data.invite.token)}`;root.querySelector('#systemInviteResult').innerHTML=`<div class="invite-result"><b>초대 링크가 만들어졌습니다.</b><code>${esc(link)}</code><small>토큰은 이 화면에서 한 번만 표시합니다.</small></div>`;root.querySelector('#systemInviteEmail').value=''}catch(err){alert(err.message)}};
  return{mount(r){root=r;root.addEventListener('submit',submit);unsub=store.subscribe((_,m)=>{if(['snapshot','user'].includes(m.type))render()})},render,destroy(){unsub?.();root?.removeEventListener('submit',submit);root=null}};
}