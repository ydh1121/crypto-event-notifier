import{holdings,holdingsSummary,holdingsHistory,findMarket,averagingPlan}from'../../shared/selectors.js';
import{n,money,pct,price,tone,esc}from'../../shared/format.js';
import{calculateAveraging,calculateDirectAverage,normalizeAveragingRows}from'../../shared/averaging.js';
import{buildHoldingPlanGuidance,buildHoldingBudgetSchedule,buildProfitProtectionGuidance}from'../../shared/holding-plan.js';
import{getMarketDetail}from'../../services/market-detail.js';
import{pageTitle,sectionTitle,emptyState,loadingState,marketSymbol,exchangeSwitch,details}from'./common.js';
export function createAssetsPage({store}){
let root=null,unsub=null,seq=0;const cache=new Map(),drafts=new Map(),budgets=new Map(),averages=new Map();const state=()=>store.get(),ui=()=>state().ui,key=(m,e)=>`${m}|${e}`,exLabel=e=>e==='upbit'?'업비트':'빗썸';
function list(){return holdings(state())}function pick(){let m=ui().assetMarket;if(!list().some(x=>x.market===m))m=list()[0]?.market||'';if(m!==ui().assetMarket)store.setUi({assetMarket:m},{scope:'v3-asset-pick'});return m}function selected(){return list().find(x=>x.market===ui().assetMarket)||null}
function draft(m){let d=drafts.get(m);if(!d){const h=list().find(x=>x.market===m);d={exchange:String(h?.exchange||'bithumb').toLowerCase()==='upbit'?'upbit':'bithumb',price:0,amount:0};drafts.set(m,d)}return d}function info(m,e){const row=findMarket(state(),e,m),data=cache.get(key(m,e))?.data||{},plan=data.trade_plan||{};return{row,data,plan,next:n(plan.next_add_price)||n(plan.expected_entry_price)||n(row?.price)}}function avg(m){if(!averages.has(m)){const saved=normalizeAveragingRows(averagingPlan(state(),m).rows);averages.set(m,saved.length?saved:[{price:0,amount_krw:0}])}return averages.get(m)}
function render(){if(!root)return;if(!state().snapshot){root.innerHTML=pageTitle('내 코인','내가 가진 코인과 손익을 확인합니다.')+loadingState();return}const rows=list(),sum=holdingsSummary(state()),m=pick();root.innerHTML=`<div class="v2-page" data-v2-assets>${pageTitle('내 코인','보유 금액과 손익을 먼저 보고 필요할 때 계산합니다.')}<section class="v2-status-strip"><div><span>현재 보유 금액</span><b>${money(sum?.value_krw)}</b><small>${rows.length}종목</small></div><div><span>평가 손익</span><b class="${tone(sum?.pnl_krw)}">${n(sum?.pnl_krw)>=0?'+':''}${money(sum?.pnl_krw)}</b><small>현재 가격 기준</small></div><div><span>투입한 금액</span><b>${money(sum?.invested_krw)}</b><small>저장된 보유 정보 기준</small></div><div><span>기록</span><b>${holdingsHistory(state()).length}개</b><small>최근 보유 변화 기록</small></div></section>${rows.length?`<section class="v2-split"><aside class="v2-list-pane"><div class="v2-list-head"><b>보유 코인</b><span>평가액 순</span></div><div class="v2-master-list">${[...rows].sort((a,b)=>n(b.value_krw)-n(a.value_krw)).map(r=>`<button data-v2-asset-market="${esc(r.market)}" class="${r.market===m?'selected':''}"><span><b>${esc(marketSymbol(r))}</b><small>${exLabel(String(r.exchange||'').toLowerCase())} · ${n(r.volume).toLocaleString('ko-KR',{maximumFractionDigits:4})}개</small></span><span><b>${money(r.value_krw)}</b><small class="${tone(r.unrealized_pnl_pct)}">${pct(r.unrealized_pnl_pct)}</small></span></button>`).join('')}</div></aside><section id="v2AssetDetail" class="v2-detail-pane"></section></section>`:emptyState('등록된 보유 코인이 없습니다.','보유 정보를 등록하면 이 화면에서 확인할 수 있습니다.')}</div>`;if(rows.length)renderDetail(m)}
function renderDetail(m){const box=root?.querySelector('#v2AssetDetail'),h=list().find(x=>x.market===m);if(!box||!h)return;const d=draft(m),x=info(m,d.exchange),loaded=cache.has(key(m,d.exchange));if(!d.price&&x.next)d.price=x.next;const guide=buildHoldingPlanGuidance({row:x.row,plan:x.plan}),profit=buildProfitProtectionGuidance({holding:h,plan:x.plan}),rows=avg(m),budget=Math.max(0,n(budgets.get(m)));box.innerHTML=`${sectionTitle(marketSymbol(h),`${exLabel(String(h.exchange||'').toLowerCase())} · 내 보유 코인`)}<section class="v2-status-strip three"><div><span>평균 매수가</span><b>${price(h.avg_price)}</b><small>저장된 값</small></div><div><span>현재가</span><b>${price(h.current_price)}</b><small class="${tone(h.unrealized_pnl_pct)}">${pct(h.unrealized_pnl_pct)}</small></div><div><span>평가 손익</span><b class="${tone(h.unrealized_pnl_krw)}">${n(h.unrealized_pnl_krw)>=0?'+':''}${money(h.unrealized_pnl_krw)}</b><small>${money(h.value_krw)}</small></div></section><section class="v2-section inset">${sectionTitle('지금 참고할 가격','실제 주문이 아니라 현재 가상 계산값입니다.',exchangeSwitch(d.exchange,'data-v2-asset-exchange'))}${loaded?`<dl class="v2-info-list grid"><div><dt>다음에 더 살 때 참고 가격</dt><dd>${guide.nextPrice?price(guide.nextPrice):'대기'}</dd></div><div><dt>수익을 지킬 참고 가격</dt><dd>${guide.targetPrice?price(guide.targetPrice):'대기'}</dd></div><div><dt>손실을 줄일 참고 가격</dt><dd>${guide.stopPrice?price(guide.stopPrice):'대기'}</dd></div><div><dt>남은 분할 횟수</dt><dd>${n(guide.remainingEntries)}회</dd></div></dl><div class="v2-note"><b>${esc(guide.status)}</b><span>${esc(guide.detail)}</span></div>`:loadingState('참고 가격을 불러오는 중입니다.')}</section><section class="v2-section inset">${sectionTitle('추가매수하면 평균 매수가가 어떻게 바뀌나','가격과 금액을 입력해 계산합니다.')}<div class="v2-form-grid"><label class="v2-field"><span>추가 매수가</span><input data-v2-direct-price type="number" min="0" step="any" value="${d.price||''}"></label><label class="v2-field"><span>추가 매수 금액</span><input data-v2-direct-amount type="number" min="0" step="10000" value="${d.amount||''}" placeholder="예: 1000000"></label></div><div class="v2-quick-money"><button data-v2-money="1000000">100만원</button><button data-v2-money="5000000">500만원</button><button data-v2-money="10000000">1,000만원</button><button data-v2-money="50000000">5,000만원</button><button data-v2-money="100000000">1억원</button></div><div id="v2DirectResult" class="v2-result-grid"></div></section><section class="v2-section inset">${sectionTitle('남은 예산 나누기','추가로 쓸 총예산을 남은 횟수에 맞춰 나눕니다.')}<label class="v2-field"><span>추가매수 총예산</span><input data-v2-asset-budget type="number" min="0" step="10000" value="${budget||''}" placeholder="예: 3000000"></label><div id="v2BudgetResult"></div></section>${details('수익을 지키는 참고 단계',`<div class="v2-step-list">${profit.stages.map((s,i)=>`<div><span>${i+1}단계</span><b>${esc(s.label)}</b><strong>${s.price?price(s.price):'조건 충족 후 계산'}</strong><small>${esc(s.action)}</small></div>`).join('')}</div>`)}${details('여러 번 나눠 살 때 평균 매수가 계산',`<div id="v2AverageRows" class="v2-average-rows">${rows.map((r,i)=>avgRow(r,i)).join('')}</div><div class="v2-actions"><button data-v2-add-avg>회차 추가</button><button data-v2-reset-avg>저장값 다시 불러오기</button></div><div id="v2AverageResult" class="v2-result-grid"></div>`)}${details('보유 기록',historyHtml())}`;updateDirect();updateBudget();updateAverage();if(!loaded)void ensureDetail(m,d.exchange)}
function avgRow(r,i){return`<div class="v2-average-row" data-v2-avg-row="${i}"><span>${i+1}회</span><input data-v2-avg-price type="number" min="0" step="any" value="${n(r.price)||''}" placeholder="가격"><input data-v2-avg-amount type="number" min="0" step="10000" value="${n(r.amount_krw)||''}" placeholder="금액"><button type="button" data-v2-remove-avg>삭제</button></div>`}function historyHtml(){const rows=holdingsHistory(state());return rows.length?`<div class="v2-compact-list">${rows.slice(-20).reverse().map(r=>`<div class="v2-compact-row static"><span><b>${money(r.value_krw)}</b><small>${new Date(n(r.ts)*1000).toLocaleString('ko-KR')}</small></span><span>${money(r.invested_krw)}</span><strong class="${tone(r.pnl_krw)}">${n(r.pnl_krw)>=0?'+':''}${money(r.pnl_krw)}</strong></div>`).join('')}</div>`:emptyState('보유 기록이 없습니다.')}
async function ensureDetail(m,e){const k=key(m,e);if(cache.has(k))return;const id=++seq;try{cache.set(k,await getMarketDetail(e,m))}catch{cache.set(k,null)}if(id===seq&&ui().assetMarket===m&&draft(m).exchange===e)renderDetail(m)}
function updateDirect(){const h=selected(),box=root?.querySelector('#v2DirectResult');if(!h||!box)return;const d=draft(h.market),r=calculateDirectAverage({volume:h.volume,avgPrice:h.avg_price,buyPrice:d.price,amountKrw:d.amount});box.innerHTML=`<span><small>현재 평균 매수가</small><b>${price(h.avg_price)}</b></span><span><small>추가매수 후</small><b>${price(r.final_avg_price)}</b></span><span><small>평균 매수가 변화</small><b>${Math.abs(n(r.avg_reduction_pct)).toFixed(2)}%</b></span><span><small>총 투입금</small><b>${money(r.final_cost_krw)}</b></span>`}
function updateBudget(){const h=selected(),box=root?.querySelector('#v2BudgetResult');if(!h||!box)return;const d=draft(h.market),x=info(h.market,d.exchange),s=buildHoldingBudgetSchedule({row:x.row,plan:x.plan,totalBudget:budgets.get(h.market)||0});box.innerHTML=s.rows.length?`<div class="v2-compact-list">${s.rows.map(r=>`<div class="v2-compact-row static"><span><b>${r.round}회</b><small>${r.priceSource==='paper_next'?'첫 참고 가격':'간격 계산값'}</small></span><span>${price(r.price)}</span><strong>${money(r.amount_krw)}</strong></div>`).join('')}</div>`:emptyState('예산을 입력하면 계산됩니다.')}
function syncAvg(){const h=selected();if(!h)return[];const rows=[...(root?.querySelectorAll('[data-v2-avg-row]')||[])].map(x=>({price:Math.max(0,n(x.querySelector('[data-v2-avg-price]')?.value)),amount_krw:Math.max(0,n(x.querySelector('[data-v2-avg-amount]')?.value))}));averages.set(h.market,rows.length?rows:[{price:0,amount_krw:0}]);return averages.get(h.market)}function updateAverage(){const h=selected(),box=root?.querySelector('#v2AverageResult');if(!h||!box)return;const r=calculateAveraging({volume:h.volume,avgPrice:h.avg_price,rows:syncAvg()});box.innerHTML=`<span><small>최종 예상 평균 매수가</small><b>${price(r.final_avg_price)}</b></span><span><small>총 투입금</small><b>${money(r.final_cost_krw)}</b></span><span><small>총 보유수량</small><b>${n(r.final_volume).toLocaleString('ko-KR',{maximumFractionDigits:6})}</b></span>`}function refreshAvg(){const h=selected(),box=root?.querySelector('#v2AverageRows');if(!h||!box)return;box.innerHTML=avg(h.market).map((r,i)=>avgRow(r,i)).join('');updateAverage()}
const click=e=>{const m=e.target.closest('[data-v2-asset-market]');if(m){store.setUi({assetMarket:m.dataset.v2AssetMarket},{scope:'v3-asset'});render();return}const ex=e.target.closest('[data-v2-asset-exchange]');if(ex){const h=selected();if(!h)return;const d=draft(h.market);d.exchange=ex.dataset.v2AssetExchange;d.price=info(h.market,d.exchange).next||n(findMarket(state(),d.exchange,h.market)?.price);renderDetail(h.market);return}const quick=e.target.closest('[data-v2-money]');if(quick){const h=selected();if(!h)return;draft(h.market).amount=n(quick.dataset.v2Money);const input=root.querySelector('[data-v2-direct-amount]');if(input)input.value=quick.dataset.v2Money;updateDirect();return}if(e.target.closest('[data-v2-add-avg]')){const h=selected();if(!h)return;const rows=syncAvg();if(rows.length<20)rows.push({price:0,amount_krw:0});averages.set(h.market,rows);refreshAvg();return}if(e.target.closest('[data-v2-reset-avg]')){const h=selected();if(!h)return;averages.delete(h.market);avg(h.market);refreshAvg();return}const rm=e.target.closest('[data-v2-remove-avg]');if(rm){const h=selected();if(!h)return;const i=n(rm.closest('[data-v2-avg-row]')?.dataset.v2AvgRow),rows=syncAvg();rows.splice(i,1);averages.set(h.market,rows.length?rows:[{price:0,amount_krw:0}]);refreshAvg()}};const input=e=>{const h=selected();if(!h)return;if(e.target.matches('[data-v2-direct-price]')){draft(h.market).price=Math.max(0,n(e.target.value));updateDirect()}else if(e.target.matches('[data-v2-direct-amount]')){draft(h.market).amount=Math.max(0,n(e.target.value));updateDirect()}else if(e.target.matches('[data-v2-asset-budget]')){budgets.set(h.market,Math.max(0,n(e.target.value)));updateBudget()}else if(e.target.matches('[data-v2-avg-price],[data-v2-avg-amount]'))updateAverage()};return{mount(r){root=r;root.addEventListener('click',click);root.addEventListener('input',input);unsub=store.subscribe((_,m)=>{if(m.type==='snapshot')render()})},render,destroy(){seq++;unsub?.();root?.removeEventListener('click',click);root?.removeEventListener('input',input);root=null}}}
