import fs from'node:fs';
const read=path=>fs.readFileSync(new URL(`../${path}`,import.meta.url),'utf8');
const index=read('public/index.html'),main=read('public/modules/main.js'),ui=read('public/modules/shared/holdings-write-v16.js'),fees=read('public/modules/shared/trading-fees-v16.js'),service=read('public/modules/services/holding-mutations-v16.js'),ownerApi=read('functions/api/holding-mutations.ts'),runtimeApi=read('functions/api/holding-mutations-runtime.ts'),migration=read('migrations/0007_holding_mutations.sql');
const consumer=fs.readFileSync(new URL('../../b3_trader/holding_mutation_consumer.py',import.meta.url),'utf8');
const userTools=fs.readFileSync(new URL('../../b3_trader/user_tools.py',import.meta.url),'utf8');
const livePatch=read('public/modules/shared/live-patch-v16.js');
const checks=[
 ['current V16B build marker',/crypto-viewer-build" content="[^"]*v16b-holdings-write/.test(index)],
 ['holdings write stylesheet remains cache-versioned',/holdings-write-v16\.css\?v=[0-9.]+/.test(index)],
 ['main entry remains cache-versioned',/main\.js\?v=[0-9.]+/.test(index)],
 ['holdings writer remains cache-versioned',/holdings-write-v16\.js\?v=[0-9.]+/.test(main)],
 ['holdings writer installed',/installHoldingsWriteV16\(\{store,root\}\)/.test(main)],
 ['Bithumb KRW fixed fee',fees.includes('bithumb_krw:0.0004')&&ownerApi.includes('bithumb_coupon_0.04pct')&&consumer.includes('bithumb_coupon_0.04pct')],
 ['Bithumb BTC fee is zero',fees.includes('bithumb_btc:0')&&ownerApi.includes('bithumb_btc_free')&&consumer.includes('bithumb_btc_free')],
 ['Upbit KRW fixed fee',fees.includes('upbit_krw:0.0005')&&ownerApi.includes('upbit_krw_0.05pct')&&consumer.includes('upbit_krw_0.05pct')],
 ['owner only mutation',ownerApi.includes('requireOwner(env, request)')],
 ['client fee ignored',ownerApi.includes('Client fee fields are intentionally ignored')],
 ['idempotency queue',migration.includes('idempotency_key TEXT NOT NULL UNIQUE')&&service.includes('idempotency_key')&&ownerApi.includes('IDEMPOTENCY_CONFLICT')],
 ['local receipt makes ACK retry idempotent',consumer.includes('holding_mutation_receipts')&&consumer.includes('SELECT result_json FROM holding_mutation_receipts')&&consumer.includes('INSERT INTO holding_mutation_receipts')],
 ['optimistic revision is mandatory',migration.includes('expected_revision REAL NOT NULL')&&ownerApi.includes('expectedRevision <= 0')&&consumer.includes('REVISION_CONFLICT')],
 ['queue status lifecycle',migration.includes("'pending','claimed','applied','rejected'")],
 ['trusted runtime auth',runtimeApi.includes('bearer(request) === env.INGEST_TOKEN')],
 ['stale claim recovery',runtimeApi.includes("status='claimed'")&&runtimeApi.includes('now - 120')],
 ['claim compare-and-set is checked',runtimeApi.includes('claim.meta.changes')&&runtimeApi.includes('Another trusted consumer won')],
 ['migration is explicit',migration.includes('CREATE TABLE IF NOT EXISTS holding_mutations')&&!ownerApi.includes('CREATE TABLE')&&!runtimeApi.includes('CREATE TABLE')],
 ['canonical local sqlite update',consumer.includes('UPDATE manual_holdings SET volume=?,avg_price=?,updated_ts=?')],
 ['canonical mutation republishes snapshot',consumer.includes('CloudflareSnapshotPublisher')&&consumer.includes('_publish_after_mutation(result)')&&consumer.includes('snapshot_publish_status')],
 ['snapshot publish failure never rolls back canonical sqlite',consumer.includes('The canonical SQLite write is already committed')],
 ['current exchange preserved',consumer.includes('EXCHANGE_CONFLICT')&&ui.includes('const holdingExchange=holding=>')&&!ui.includes("holding.exchange||'bithumb'")],
 ['legacy exchange is owner-selected',ui.includes('data-holding-edit-exchange')&&ui.includes('exchangeDrafts')&&ui.includes('거래소 선택 필요')],
 ['legacy exchange is canonically backfilled',consumer.includes('exchange_backfilled')&&consumer.includes('SET volume=?,avg_price=?,exchange=?,updated_ts=?')],
 ['canonical confirmation includes exchange',ui.includes('exchangeOk=!result.exchange||holdingExchange(holding)===String(result.exchange).toLowerCase()')],
 ['mutation history can be listed after reload',service.includes('listHoldingMutations')&&service.includes("fetch('/api/holding-mutations'")],
 ['latest mutation status is restored per market',ui.includes('hydrateMutationHistory')&&ui.includes('response.mutations')&&ui.includes('seen.has(market)')],
 ['mutation history is isolated by owner session',ui.includes("historyActor")&&ui.includes('pendingByMarket.clear()')&&ui.includes("store.get().user?.id")],
 ['manual holdings support BTC quote keys',ownerApi.includes('(?:\\/[A-Z0-9]+)?')&&ui.includes('(?:\\/[A-Z0-9]+)?')&&userTools.includes('normalize_holding_market')],
 ['BTC averaging write waits for quote-aware calculator',ownerApi.includes('QUOTE_AWARE_AVERAGING_REQUIRED')&&consumer.includes('QUOTE_AWARE_AVERAGING_REQUIRED')&&ui.includes('BTC 마켓 물타기 실제 반영')],
 ['live asset rail preserves unspecified exchange',livePatch.includes("exchange==='bithumb'?'빗썸':'거래소 미지정'")&&!livePatch.includes("==='upbit'?'업비트':'빗썸'")],
 ['selected averaging rounds',ui.includes('data-avg-actual-apply')&&ui.includes('data-apply-selected-rounds')&&consumer.includes('apply_averaging')],
 ['actual holding edit',ui.includes('data-holding-edit-volume')&&ui.includes('data-holding-edit-avg')&&ui.includes('data-save-holding')],
 ['take profit target',ui.includes('data-take-profit-price')],
 ['take profit recalculates after edit',ui.includes("event.target.matches('[data-take-profit-price]')")&&ui.includes('setTimeout(rerender,0)')],
 ['calculator row replacement restores controls',ui.includes("[data-add-avg-row],[data-add-recommended-row],[data-reset-avg],[data-remove-avg]")],
 ['existing buy fee is visible',ui.includes('기존 매수 수수료(추정)')&&ui.includes('originalBuyFee=feeReady?tradingFee(startCost,exchange):0')],
 ['additional buy fee is separate',ui.includes('추가매수 수수료')&&ui.includes('additionalBuyFee=feeReady?tradingFee(addition,exchange):0')],
 ['exit fee and total fee are complete',ui.includes('매도 수수료')&&ui.includes('총 수수료')&&ui.includes('totalFee=totalBuyFee+sellFee')],
 ['net profit includes all buy fees',ui.includes('cashBasis=feeReady?finalCost+totalBuyFee:0')&&ui.includes('profit=feeReady&&target>0?netProceeds-cashBasis:0')],
 ['final value and receipt are visible',ui.includes('익절 최종 가치')&&ui.includes('최종 수령액')&&ui.includes('순수익률')],
 ['stale read only management note is removed',ui.includes("detail.querySelector('.local-management-note')?.remove()")],
 ['canonical confirmation gate',ui.includes('DB 적용됨 · 스냅샷 확인중')&&ui.includes("?'적용됨':")],
 ['consumer not auto-started from frontend',!main.includes('holding_mutation_consumer')],
];
const failed=checks.filter(([,ok])=>!ok);if(failed.length){console.error('V16_HOLDINGS_WRITE=FAIL');for(const[name]of failed)console.error(`- ${name}`);process.exit(1)}console.log('V16_HOLDINGS_WRITE=PASS');
